- content_for :title do
  Sign In Options

- if @authentications
  - unless @authentications.empty?
    %p
      %strong You have signed in with these services:

    .authentications
      - @authentications.each do |authentication|
        .authentication
          = image_tag "authentications/#{authentication.provider}_32.png", size: '32x32'
          .provider= authentication.provider_name
          .uid= authentication.uid
          = link_to 'X', authentication_path(authentication), confirm: 'Are you sure you want to remove this authentication option?', method: :delete, class: 'remove'
      .clear
  %p
    %strong Add another service to sign in with:
- else
  %p
    %strong Sign in through one of these services:

- Authentication::PROVIDERS.each do |provider|
  = link_to "/auth/#{provider}", class: 'auth_provider' do
    = image_tag "authentications/#{provider}_64.png", alt: provider.titleize
    = provider.titleize

.clear
